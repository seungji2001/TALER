<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clone.practice">
    <!-- permission -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practice"
        tools:targetApi="31">
        <activity
            android:name=".SelectTextRecordActivity"
            android:exported="false" />
        <activity
            android:name=".SelfWriteActivity"
            android:exported="false"
            android:label="직접 입력하세요!"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity
            android:name=".EnrollBookActivity"
            android:exported="false"
            android:label="추천 동화 목록"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectBookActivity"
            android:exported="false" />
        <activity
            android:name=".CharActivity"
            android:exported="false" /> <!-- SMS등록 receiver 등록하기 -->
        <receiver
            android:name=".SMSReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".NumActivity"
            android:exported="true" />
        <activity
            android:name=".TalerMain"
            android:exported="false" />
    </application>

</manifest>